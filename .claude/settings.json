{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -qE '^cargo (build|b)'; then pkill -f hitch-daemon 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
