// Pure client-side SPA in the Tauri webview: no SSR, no prerender. All routes
// render in the browser where `@tauri-apps/api` is available.
export const ssr = false;
export const prerender = false;
