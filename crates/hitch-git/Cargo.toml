[package]
name = "hitch-git"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Git reads (libgit2), writes (system git), worktrees, and PRs (gh)."

[dependencies]
hitch-core.workspace = true
git2 = "0.20"
libc = "0.2"
tempfile = "3"
